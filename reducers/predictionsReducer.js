import * as types from '../actions/actionTypes.js';
import * as action from '../actions/actions.js';

const initialState = {
  value: 0,
}

const predictionsReducer = (state = initialState, action) => {
  // do all the switch / case stuff
  return state;
};

export default predictionsReducer;